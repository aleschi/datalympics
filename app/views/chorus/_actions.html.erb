
<% if  !@actions_show.empty?%>
<div class="SD-topo-box">
<div class="SD-topo-box-title2">Visualisation au niveau des actions des UO (<%= @uo_actions.length %>)</div><br>
   <div class="chorus-dropdown-box">
   <div class="dropdown dropdown-chorus">
     <button class="btn btn-secondary dropdown-toggle dropdown-select-chorus" type="button" id="dropdownMenuButton2" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Sélectionner une action <i class="fas fa-chevron-down"></i></button>
      <div class="dropdown-menu dropwdown-menu-chorus" aria-labelledby="dropdownMenuButton2">
          <%= form_with url: select_action_path(:uo_id => @uos_show, :date => @dates, :budget => @budget), id: "action_form" do |f|%>
        <% (0..@uo_actions.length-1).each do |i|%>
          <label><%= f.check_box :id,{:multiple => true}, @uo_actions[i],nil %>  <%= @uo_actions[i]%></label>
        <% end %>
       <% end %>
      </div>
    </div>
  </div> 
  <div id="actions2">
     <%= render 'chorus/actions2'%>
  </div>
 

</div>

<script>
  $('#action_form input').click(function(){
$('#action_form').submit();
});
</script>
<script>
  var elementactionid = '#id_'+'<%= @uo_action %>'
  $(elementactionid).prop('checked', true);

</script>

 <% end %>