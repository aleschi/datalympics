<div class="O_page">
  <div class="O_home_button">
    <%= link_to collectivites_path do %><i class="fas fa-chevron-left"></i>Retour aux collectivités <%end %>
  </div>
   <h1 class="SD_title"><%= @solideo_financement.financeur %> </h1>


  <div class="O-topo-box">
   <div class="O-topo-box-infos"> 
      <%= pie_chart(@financement.unscope(:order).group(:categorie).sum('montant_prevu'), library: {
      chart: {
           backgroundColor: '#fff',
        style:{
          fontFamily: "Spectral",
          maxWidth: "500px",
          }
        },
   title: {
          text:'Répartition des financements de la collectivité (2018-2025)',
          margin:40,
          style: {
            color: '#8F8F8F',
            fontSize: '16px',
            fontStyle: 'italic',
            }
        },
      colors: ['#4F82CE', "#AED9E6" , "#0287D1",'#587577' ],
        tooltip: {
   
          pointFormat: 'Montant : <b>{point.y:,.0f}€</b><br>',
        
        },

        plotOptions: {
            pie: {
                allowPointSelect: true,
              minSize: 200,
               # cursor: 'pointer',
                dataLabels: {
                    enabled: true,
                    format: '<em>{point.name}<br>{point.y:,.0f}€ - {point.percentage:.1f} %</em>',
                    style: {
                      color: "#8F8F8F",
                      fontSize: "12px",
                      fontWeight: "400",

                      }
                }
            }
        },
      }) %>

    </div>
    <% if !@ouvrages.nil? && !@ouvrages.empty? %>    
  <div class="width-50">
    <div class="SD-topo-box-title"><span>Ouvrages financés</span></div><br><br>
     <div class="SF-topo-financeurs-box">
      <div class="collectivites-ouvrages-name"></div>
      <div class="SF-topo-financeurs-budget"><div class="SF-topo-financeurs-budget-prevu"><em>Fin. prévus €</em></div><div class="SF-topo-financeurs-budget-actuel"> | <em>Fin. actuels €</em></div></div>
    </div> <br><br>
    <% @ouvrages.each do |ouvrage| %>
    <div class="SF-topo-financeurs-box">
      <div class="collectivites-ouvrages-name"><%= link_to ouvrage_path(ouvrage) do %><%= Ouvrage.find(ouvrage).name %> <% end %></div>
      <div class="SF-topo-financeurs-budget"><div class="SF-topo-financeurs-budget-prevu"><%= number_with_delimiter(OuvragesFinancement.where('name = ? AND ouvrage_id = ?',@solideo_financement.financeur, ouvrage).sum('montant_prevu').to_i, :delimiter => ' ')%> €</div><div class="SF-topo-financeurs-budget-actuel"> |  <%= number_with_delimiter(OuvragesFinancement.where('name = ? AND ouvrage_id = ?',@solideo_financement.financeur, ouvrage).sum('montant').to_i, :delimiter => ' ') %> €</div></div>
    </div>
    <% end %>
  </div>
  <% end %>
</div><br><br>
  
<div class="SD-topo-box">
  <div class="SD-topo-box-title">
        <span>Suivi des Financements</span>
      </div><br><br>
  <div class="budget-topo_chart-box"> 
   
    <div class="budget-topo-chart">
      
    
 
      <div id="container2">
 
      </div>
      <div class="SF-topo-explication">
         <% if @financement.sum('montant') > @financement.where('date <= ?', Date.today).sum('montant_prevu') %><span class="positive"> + <%= number_with_delimiter((@financement.sum('montant')-@financement.where('date <= ?', Date.today).sum('montant_prevu')).to_i, :delimiter => ' ')%> € </span><% else %><span class="negative"> - <%= number_with_delimiter((@financement.where('date <= ?', Date.today).sum('montant_prevu')-@financement.sum('montant')).to_i, :delimiter => ' ')%> €</span><% end %> par rapport aux financements prévus à date 
      </div>
    </div>
    <div class="budget-topo-chart">
      <%= column_chart [{name: "Financements prévus", data: @financement.unscope(:order).group_by_year(:date).sum('montant_prevu')},{name: "Financements reçus", data: @financement.unscope(:order).group_by_year(:date).sum('montant')}], library: {
        chart:{
            type: 'column',
            backgroundColor: '#fff',
            style:{
            fontFamily: "Spectral",
               maxWidth: "500px",
            },
         
            },
      
        title: {
          text:'Financements prévus et attribués par année en €',
          margin:40,
          style: {
                  color: "#8F8F8F",
                  fontWeight: "400",
            fontStyle: 'italic',
          fontSize: '16px',
                  },
        },
      xAxis:  {
        type: 'datetime',
        },
        
      yAxis: {
          gridLineColor: '#fff', 
         },
        colors: ["#8F8F8F", "#48AF5C"],
     
  
        plotOptions: {
          column: {
           pointWidth: 15,
           tooltip: {        
              backgroundColor: "#fff",        
              style: {
                  fontSize: '12px',     
              },
              xDateFormat: '%Y',
              useHTML: true,
              pointFormat: '<span style="color:{point.color}">●</span> {series.name}: <b>{series.name}</b><br/>',

             },
           },
    
          },


          }
      %>
    </div>
    
  </div>
  <br><br>
</div>
  
<div  class="SD-table-box">
   <div class="SD-topo-box-title">
        <span>Suivi des Financements par pole</span>
    </div><br><br>
    <div class="row">
      <div class="col-md-6">
        <div class="SD-topo-box-title">
        <span>Ouvrages</span>
        </div><br><br>
        
    <div>
    <%= line_chart [{name: "Financement prévus", data: @financement.where('categorie = ?', "ouvrages").unscope(:order).group_by_year(:date).sum('montant_prevu')}, {name: "Financement attribués", data: @financement.where('categorie = ?', "ouvrages").unscope(:order).group_by_year(:date).sum('montant') } ], library: {
      chart:{
            backgroundColor: '#fff',
           
        style:{
          fontFamily: "Spectral",
         
          }
          },

      title: {
        text:'Financements des ouvrages',
        margin:40,
        style: {
                  color: "#8F8F8F",
                  fontWeight: "400",
          fontStyle: 'italic',
          fontSize: '12px',
                  },
      },
      yAxis: {
        gridLineColor: '#E6F0FF', 
           labels: {
               format: '${value}'
           }
       },
          tooltip: {
            pointFormat: 'Total: <b>{point.y}</b>',
            xDateFormat: '%Y',
            valuePrefix: '$'
          }
        }
    %>
  </div>
      </div>
      <div class="col-md-6">
         <div class="SD-topo-box-title">
        <span>Fonctionnement</span>
      </div><br><br>
        
    <div>
    <%= line_chart [{name: "Financement prévus", data: @financement.where('categorie = ?', "fonctionnement").unscope(:order).group_by_year(:date).sum('montant_prevu')}, {name: "Financement attribués", data: @financement.where('categorie = ?', "fonctionnement").unscope(:order).group_by_year(:date).sum('montant') } ], library: {
      chart:{
            backgroundColor: '#fff',
           
        style:{
          fontFamily: "Spectral",
         
          }
          },

      title: {
        text:'Financements Fonctionnement',
        margin:40,
        style: {
                  color: "#8F8F8F",
                  fontWeight: "400",
          fontStyle: 'italic',
          fontSize: '12px',
                  },
      },
      yAxis: {
        gridLineColor: '#E6F0FF', 
           labels: {
               format: '${value}'
           }
       },
          tooltip: {
            pointFormat: 'Total: <b>{point.y}</b>',
            xDateFormat: '%Y',
            valuePrefix: '$'
          }
        }
    %>
  </div>
      </div>
    </div><br><br>
  <div class="row">
    <div class="col-md-6">
       <div class="SD-topo-box-title">
        <span>Innovation</span>
      </div><br><br>
      
    <div>
    <%= line_chart [{name: "Financements prévus", data: @financement.where('categorie = ?', "innovation").unscope(:order).group_by_year(:date).sum('montant_prevu')}, {name: "Financement attribués", data: @financement.where('categorie = ?', "innovation").unscope(:order).group_by_year(:date).sum('montant') } ], library: {
      chart:{
            backgroundColor: '#fff',
           
        style:{
          fontFamily: "Spectral",
         
          }
          },

      title: {
        text:'Financements Innovation',
        margin:40,
        style: {
                  color: "#8F8F8F",
                  fontWeight: "400",
          fontStyle: 'italic',
          fontSize: '12px',
                  },
      },
      yAxis: {
        gridLineColor: '#E6F0FF', 
           labels: {
               format: '${value}'
           }
       },
          tooltip: {
            pointFormat: 'Total: <b>{point.y}</b>',
            xDateFormat: '%Y',
            valuePrefix: '$'
          }
        }
    %>
  </div>
      </div>
    <div class="col-md-6">
       <div class="SD-topo-box-title">
        <span>Réserve</span>
      </div><br><br>
      
    <div>
    <%= line_chart [{name: "Financements prévus", data: @financement.where('categorie = ?', "reserve").unscope(:order).group_by_year(:date).sum('montant_prevu')}, {name: "Financement attribués", data: @financement.where('categorie = ?', "reserve").unscope(:order).group_by_year(:date).sum('montant') } ], library: {
      chart:{
            backgroundColor: '#fff',
           
        style:{
          fontFamily: "Spectral",
         
          }
          },

      title: {
        text:'Financements Réserve',
        margin:40,
        style: {
                  color: "#8F8F8F",
                  fontWeight: "400",
          fontStyle: 'italic',
          fontSize: '12px',
                  },
      },
      yAxis: {
        gridLineColor: '#E6F0FF', 
           labels: {
               format: '${value}'
           }
       },
          tooltip: {
            pointFormat: 'Total: <b>{point.y}</b>',
            xDateFormat: '%Y',
            valuePrefix: '$'
          }
        }
    %>
  </div>
      </div>
  </div><br>
 
  </div>
</div>
</div>

 <script>
  // Uncomment to style it like Apple Watch

function renderIcons() {

    // Move icon
    if (!this.series[0].icon) {
        this.series[0].icon = this.renderer.path(['M', -8, 0, 'L', 8, 0, 'M', 0, -8, 'L', 8, 0, 0, 8])
            .attr({
                stroke: '#303030',
                'stroke-linecap': 'round',
                'stroke-linejoin': 'round',
                'stroke-width': 2,
                zIndex: 10
            })
            .add(this.series[2].group);
    }
    this.series[0].icon.translate(
        this.chartWidth / 2 - 10,
        this.plotHeight / 2 - this.series[0].points[0].shapeArgs.innerR -
            (this.series[0].points[0].shapeArgs.r - this.series[0].points[0].shapeArgs.innerR) / 2
    );

  

   
}


Highcharts.chart('container2', {

    chart: {
      backgroundColor: '#fff',
        type: 'solidgauge',
        maxWidth: '500px',
      height:'300px',
      style:{
          fontFamily: "Spectral",
          },
       events: {
            render: renderIcons
        }
      
    },
 
  title: {
    text: "Financements attribués par la collectivité",
    style: {
          color: '#8F8F8F',
      fontStyle: 'italic',
          fontSize: '16px',
        
          }
  },
  tooltip: {
        borderWidth: 0,
        backgroundColor: 'none',
        shadow: false,
        style: {
            fontSize: '12px',
          color: '#8F8F8F',
        },
        //valueSuffix: '€',
        pointFormat: '',
        positioner: function (labelWidth) {
            return {
                x: (this.chart.chartWidth - labelWidth) / 2,
                y: (this.chart.plotHeight / 2) + 35
            };
        }
    },

    pane: {
        startAngle: 0,
        endAngle: 360,
        background: [{ // Track for Move
            outerRadius: '90%',
            innerRadius: '80%',
            backgroundColor: 'rgba(72,175,92,0.3)',
            borderWidth: 0
        }]
    },

    yAxis: {
        min: 0,
        max: <%= @financement.sum('montant_prevu')%>,
        lineWidth: 0,
        tickPositions: [<%= @financement.sum('montant_prevu')%>],
      tickInterval: <%= @financement.sum('montant_prevu')%>,
      minorTickInterval: null,
      tickAmount:1,
       labels: {
            step: <%= @financement.sum('montant_prevu')%>,
            rotation: 'auto',
         style: {
           fontSize: '12px',
          color: '#8F8F8F',
          
         },
         y: -30,
        },
    },

    plotOptions: {
        solidgauge: {
          
          tooltip: "",
          
            dataLabels: {
              enabled: true,
              borderWidth: 0,
              backgroundColor: 'none',
              shadow: false,
              style: {
                  fontSize: '14px',
                color: '#48AF5C',
              },
              valueSuffix: '€',
              format: '<span style="color:#8F8F8F; font-size:12px;font-weight:400">{series.name} :</span><br><span>{point.y:,.0f} €</span>',
            verticalAlign: 'middle',
        
            },
            linecap: 'round',
            stickyTracking: false,
            rounded: true,
         
        }
    },


    series: [{
        name: 'Financements attribués',
        data: [{
            color: '#48AF5C',
            radius: '90%',
            innerRadius: '80%',
            y: <%= @financement.sum('montant')%>
        }]
    }]
});

  </script>
